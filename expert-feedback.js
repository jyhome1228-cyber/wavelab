(()=>{const form=document.querySelector('[data-concern-form]');if(!form)return;function popup(){let el=document.querySelector('[data-feedback-ui-popup]');if(el)return el;el=document.createElement('div');el.className='feedback-ui-popup';el.dataset.feedbackUiPopup='';el.innerHTML='<div class="feedback-ui-card"><span>✦</span><h3>고민 남기기 UI가 준비되었습니다.</h3><p>현재는 화면 구성 단계라 작성 내용이 저장되지 않습니다. Firebase 연결 후 선택한 전문가에게 전달되고 마이페이지에서 답변을 확인할 수 있게 연결할 예정입니다.</p><button type="button">확인</button></div>';document.body.appendChild(el);const close=()=>{el.classList.remove('is-open');document.body.style.overflow=''};el.querySelector('button').addEventListener('click',close);el.addEventListener('click',event=>{if(event.target===el)close()});return el}form.addEventListener('submit',event=>{event.preventDefault();const title=form.elements.title?.value.trim();const content=form.elements.content?.value.trim();const agree=form.elements.agree?.checked;if(!title||!content){form.elements[!title?'title':'content']?.focus();return}if(!agree){form.elements.agree?.focus();return}const el=popup();el.classList.add('is-open');document.body.style.overflow='hidden'});document.querySelectorAll('.expert-filter button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.expert-filter button').forEach(item=>item.classList.remove('is-active'));button.classList.add('is-active')}));})();