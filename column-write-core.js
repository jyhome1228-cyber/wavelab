import { auth, db } from './firebase-config.js';
import { onAuthStateChanged } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js';
import { addDoc, collection, doc, getDoc, serverTimestamp, updateDoc } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js';
const form=document.querySelector('[data-column-form]'),statusBox=document.querySelector('[data-column-write-status]'),notice=document.querySelector('[data-column-notice]'),titleEl=document.querySelector('[data-write-title]'),draftButton=document.querySelector('[data-save-draft]'),id=new URLSearchParams(location.search).get('id');let user=null,profile=null;
function showStatus(title,text,link='columnist-apply.html'){statusBox.hidden=false;statusBox.innerHTML=`<div class="column-editor-gate"><strong>${title}</strong><p>${text}</p><a href="${link}">칼럼니스트 신청</a></div>`}
function fill(data){form.elements.category.value=data.category||'디자인';form.elements.thumbnailUrl.value=data.thumbnailUrl||'';form.elements.title.value=data.title||'';form.elements.summary.value=data.summary||'';form.elements.content.value=data.content||''}
async function save(status){if(!user||!profile)return;const button=status==='draft'?draftButton:form.querySelector('button[type=submit]'),data=new FormData(form),payload={authorUid:user.uid,authorName:profile.penName||user.displayName||'칼럼니스트',authorBio:profile.bio||'',authorExpertise:profile.expertise||'',verifiedColumnist:true,category:String(data.get('category')||'').trim(),thumbnailUrl:String(data.get('thumbnailUrl')||'').trim(),title:String(data.get('title')||'').trim(),summary:String(data.get('summary')||'').trim(),content:String(data.get('content')||'').trim(),contentFormat:'html',status,updatedAt:serverTimestamp()};if(!payload.title||!payload.summary||!payload.content){notice.textContent='제목, 요약, 본문을 모두 입력해 주세요.';return}if(payload.content.length>850000){notice.textContent='본문 또는 이미지 용량이 너무 큽니다.';return}button.disabled=true;notice.textContent='저장 중...';try{if(id){await updateDoc(doc(db,'columns',id),payload);notice.textContent=status==='draft'?'임시저장되었습니다.':'검토 요청을 보냈습니다.'}else{const saved=await addDoc(collection(db,'columns'),{...payload,createdAt:serverTimestamp(),publishedAt:null});location.replace(`column-write.html?id=${saved.id}`);return}}catch(error){notice.textContent=error.code==='permission-denied'?'승인된 칼럼니스트만 작성할 수 있습니다.':'저장 중 오류가 발생했습니다.'}button.disabled=false}
draftButton?.addEventListener('click',()=>save('draft'));form?.addEventListener('submit',event=>{event.preventDefault();save('review')});
onAuthStateChanged(auth,async current=>{user=current;if(!user){location.replace(`login.html?next=${encodeURIComponent('column-write.html'+location.search)}`);return}try{const profileSnap=await getDoc(doc(db,'columnistProfiles',user.uid));if(!profileSnap.exists()||profileSnap.data().status!=='approved'){showStatus('칼럼니스트 승인이 필요합니다.','칼럼 작성은 WAVELAB 인증 칼럼니스트만 이용할 수 있습니다. 먼저 신청서를 제출해 주세요.');return}profile=profileSnap.data();if(id){const postSnap=await getDoc(doc(db,'columns',id));if(!postSnap.exists()){showStatus('글을 찾을 수 없습니다.','삭제되었거나 존재하지 않는 글입니다.','mypage.html#columns');return}if(postSnap.data().authorUid!==user.uid){showStatus('수정 권한이 없습니다.','본인이 작성한 글만 수정할 수 있습니다.','column.html');return}fill(postSnap.data());titleEl.textContent='칼럼 수정'}statusBox.hidden=true;form.hidden=false}catch{showStatus('작성 권한을 확인하지 못했습니다.','잠시 후 다시 시도해 주세요.')}});