const editor=document.querySelector('[data-column-editor]');
const hidden=document.querySelector('[data-column-content]');
if(editor&&hidden){
  const sync=()=>{hidden.value=editor.innerHTML.trim()};
  editor.addEventListener('input',sync);
  const hydrate=setInterval(()=>{if(hidden.value&&!editor.innerHTML.trim()){editor.innerHTML=hidden.value.includes('<')?hidden.value:hidden.value.split(/\n\s*\n/).map(p=>`<p>${p.replace(/\n/g,'<br>')}</p>`).join('');sync()}if(!form?.hidden)clearInterval(hydrate)},300);
  document.querySelectorAll('[data-command]').forEach(button=>button.addEventListener('click',()=>{let value=null;if(button.dataset.command==='createLink'){value=window.prompt('연결할 주소를 입력해 주세요.','https://');if(!value)return}editor.focus();document.execCommand(button.dataset.command,false,value);sync()}));
  document.querySelector('[data-format-block]')?.addEventListener('change',event=>{editor.focus();document.execCommand('formatBlock',false,event.target.value);sync()});
  document.querySelector('[data-font-size]')?.addEventListener('change',event=>{editor.focus();document.execCommand('fontSize',false,event.target.value);sync()});
  document.querySelectorAll('[data-align]').forEach(button=>button.addEventListener('click',()=>{editor.focus();const name=button.dataset.align;document.execCommand(`justify${name.charAt(0).toUpperCase()}${name.slice(1)}`,false,null);sync()}));
  document.querySelector('[data-line-height]')?.addEventListener('change',event=>{const block=window.getSelection()?.anchorNode?.parentElement?.closest('p,h2,h3,blockquote,li');if(block)block.style.lineHeight=event.target.value;sync()});
  document.querySelector('[data-letter-spacing]')?.addEventListener('change',event=>{const block=window.getSelection()?.anchorNode?.parentElement?.closest('p,h2,h3,blockquote,li');if(block)block.style.letterSpacing=event.target.value;sync()});
  document.querySelector('[data-editor-image]')?.addEventListener('change',event=>{const file=event.target.files?.[0];if(!file)return;if(editor.querySelectorAll('img').length>=3){alert('본문 이미지는 최대 3장까지 등록할 수 있습니다.');return}if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>8*1024*1024){alert('8MB 이하 JPG, PNG, WEBP 이미지만 등록할 수 있습니다.');return}const reader=new FileReader();reader.onload=()=>{const image=new Image();image.onload=()=>{const max=1200,ratio=Math.min(1,max/image.width),canvas=document.createElement('canvas');canvas.width=Math.round(image.width*ratio);canvas.height=Math.round(image.height*ratio);canvas.getContext('2d').drawImage(image,0,0,canvas.width,canvas.height);editor.focus();document.execCommand('insertImage',false,canvas.toDataURL('image/jpeg',.68));sync()};image.src=String(reader.result||'')};reader.readAsDataURL(file);event.target.value=''});
}
const form=document.querySelector('[data-column-form]');